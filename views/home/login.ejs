<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Viettech Admin - Đăng Nhập</title>
    <link rel="icon" href="/uploads/mini_logo.ico" type="image/png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #1a237e;
            --secondary-color: #ff6d00;
            --background-color: #f8f9fa;
            --text-color: #424242;
            --light-color: #ffffff;
            --border-color: #e0e0e0;
            --danger-color: #d32f2f;
            --success-color: #2e7d32;
        }

        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #e4e8f0 100%);
            font-family: 'Segoe UI', Roboto, 'Helvetica Neue', sans-serif;
            height: 100vh;
            display: flex;
            align-items: center;
            overflow-x: hidden;
        }

        .viettech-container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
        }

        .viettech-logo {
            text-align: center;
            margin-bottom: 2rem;
            animation: fadeInDown 0.8s ease-out;
        }

        .viettech-logo h1 {
            font-size: 2.8rem;
            font-weight: 700;
            margin-bottom: 0;
            color: var(--primary-color);
            letter-spacing: -0.5px;
        }

        .viettech-logo span {
            color: var(--secondary-color);
            position: relative;
        }

        .viettech-logo span::after {
            content: "";
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background-color: var(--secondary-color);
            border-radius: 2px;
        }

        .logo-tagline {
            font-size: 1.1rem;
            color: var(--text-color);
            opacity: 0.8;
            margin-top: 0.5rem;
        }

        .admin-card {
            background-color: var(--light-color);
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
            animation: fadeInUp 0.8s ease-out;
            transform: translateY(0);
        }

        .admin-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
        }

        .card-header {
            background: linear-gradient(135deg, var(--primary-color) 0%, #303f9f 100%);
            color: white;
            padding: 2rem;
            position: relative;
        }

        .card-header h2 {
            font-size: 1.7rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }

        .card-header p {
            margin-bottom: 0;
            opacity: 0.9;
            font-size: 1rem;
        }

        .admin-icon {
            position: absolute;
            top: 1.8rem;
            right: 2rem;
            width: 56px;
            height: 56px;
            background-color: rgba(255, 255, 255, 0.15);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            transition: all 0.3s ease;
        }

        .admin-icon:hover {
            transform: rotate(15deg);
        }

        .admin-icon i {
            font-size: 1.6rem;
            color: white;
        }

        .card-body {
            padding: 2.5rem;
        }

        .form-label {
            font-weight: 500;
            color: var(--text-color);
            margin-bottom: 0.6rem;
        }

        .form-control {
            padding: 0.9rem 1rem;
            border: 1px solid var(--border-color);
            border-radius: 8px;
            transition: all 0.3s;
            font-size: 1rem;
        }

        .form-control:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 0.25rem rgba(26, 35, 126, 0.15);
        }

        .input-group {
            border-radius: 8px;
            overflow: hidden;
        }

        .input-group-text {
            background-color: #f8f9fa;
            border: 1px solid var(--border-color);
            color: var(--text-color);
            padding-left: 1rem;
            padding-right: 1rem;
        }

        .btn-viettech {
            background: linear-gradient(135deg, var(--primary-color) 0%, #303f9f 100%);
            color: white;
            border: none;
            padding: 0.9rem 1.5rem;
            font-weight: 500;
            letter-spacing: 0.5px;
            transition: all 0.3s;
            width: 100%;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(26, 35, 126, 0.25);
            position: relative;
            overflow: hidden;
        }

        .btn-viettech::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: all 0.6s;
        }

        .btn-viettech:hover {
            background: linear-gradient(135deg, #0d1b69 0%, #232f78 100%);
            transform: translateY(-2px);
            box-shadow: 0 6px 15px rgba(26, 35, 126, 0.35);
        }

        .btn-viettech:hover::before {
            left: 100%;
        }

        .tech-footer {
            margin-top: 2.5rem;
            text-align: center;
            color: var(--text-color);
            opacity: 0.8;
            font-size: 0.9rem;
            animation: fadeInUp 1s ease-out;
        }

        .tech-footer p:last-child {
            margin-bottom: 0;
        }

        .tech-icon {
            margin-right: 0.5rem;
        }

        .dashboard-features {
            display: flex;
            flex-wrap: wrap;
            margin-top: 2rem;
            gap: 0.8rem;
            justify-content: center;
        }

        .feature-badge {
            padding: 0.6rem 1.2rem;
            background-color: rgba(26, 35, 126, 0.08);
            color: var(--primary-color);
            border-radius: 50px;
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            transition: all 0.3s;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.05);
        }

        .feature-badge:hover {
            transform: translateY(-3px);
            box-shadow: 0 5px 10px rgba(0, 0, 0, 0.1);
        }

        .orange-badge {
            background-color: rgba(255, 109, 0, 0.08);
            color: var(--secondary-color);
        }

        .alert {
            border-radius: 8px;
            padding: 1rem 1.2rem;
            margin-top: 1.5rem;
            border: none;
            display: flex;
            align-items: center;
        }

        .alert-danger {
            background-color: rgba(211, 47, 47, 0.08);
            color: var(--danger-color);
            border-left: 4px solid var(--danger-color);
        }

        .forgot-link {
            color: var(--primary-color);
            text-decoration: none;
            font-size: 0.95rem;
            transition: all 0.3s;
            font-weight: 500;
        }

        .forgot-link:hover {
            color: var(--secondary-color);
            text-decoration: underline;
        }

        .form-check-input:checked {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
        }

        /* Animations */
        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .card-body {
                padding: 1.8rem;
            }

            .viettech-logo h1 {
                font-size: 2.3rem;
            }

            .admin-icon {
                width: 48px;
                height: 48px;
                top: 1.5rem;
                right: 1.5rem;
            }

            .admin-icon i {
                font-size: 1.4rem;
            }

            .card-header {
                padding: 1.8rem;
            }

            .card-header h2 {
                font-size: 1.5rem;
            }

            .dashboard-features {
                justify-content: flex-start;
            }
        }
    </style>
</head>

<body>
    <div class="container viettech-container">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="viettech-logo">
                    <h1>Viet<span>Tech</span></h1>
                    <p class="logo-tagline">Hệ thống quản trị cửa hàng điện tử</p>
                </div>

                <div class="admin-card">
                    <div class="card-header">
                        <h2>Đăng nhập quản trị</h2>
                        <p>Đăng nhập để quản lý cửa hàng điện tử Viettech</p>
                        <div class="admin-icon">
                            <i class="fas fa-user-shield"></i>
                        </div>
                    </div>

                    <div class="card-body">
                        <form id="loginForm">
                            <div class="mb-4">
                                <label for="username" class="form-label">Tên đăng nhập</label>
                                <div class="input-group">
                                    <span class="input-group-text"><i class="fas fa-user"></i></span>
                                    <input type="text" class="form-control" id="email" name="email"
                                        placeholder="Nhập tên đăng nhập" required>
                                </div>
                            </div>

                            <div class="mb-4">
                                <label for="password" class="form-label">Mật khẩu</label>
                                <div class="input-group">
                                    <span class="input-group-text"><i class="fas fa-lock"></i></span>
                                    <input type="password" class="form-control" id="password" name="password"
                                        placeholder="Nhập mật khẩu" required>
                                    <span class="input-group-text" id="togglePassword" style="cursor: pointer;">
                                        <i class="fas fa-eye"></i>
                                    </span>
                                </div>
                            </div>

                            <div class="d-flex justify-content-between align-items-center mb-4">
                                <div class="form-check">
                                    <input class="form-check-input" type="checkbox" id="rememberMe">
                                    <label class="form-check-label" for="rememberMe">
                                        Ghi nhớ đăng nhập
                                    </label>
                                </div>

                                <a href="#" class="forgot-link">Quên mật khẩu?</a>
                            </div>

                            <button type="submit" class=" btn-viettech">
                                <i class="fas fa-sign-in-alt me-2"></i> Đăng nhập
                            </button>

                            <div id="errorMessage" class="alert alert-danger" style="display: none;">
                                <i class="fas fa-exclamation-circle me-2"></i>
                                <span id="errorText"></span>
                            </div>

                            <div class="dashboard-features">
                                <div class="feature-badge">
                                    <i class="fas fa-box tech-icon"></i> Quản lý sản phẩm
                                </div>
                                <div class="feature-badge orange-badge">
                                    <i class="fas fa-shopping-cart tech-icon"></i> Đơn hàng
                                </div>
                                <div class="feature-badge">
                                    <i class="fas fa-users tech-icon"></i> Khách hàng
                                </div>
                                <div class="feature-badge orange-badge">
                                    <i class="fas fa-chart-line tech-icon"></i> Báo cáo
                                </div>
                            </div>
                        </form>
                    </div>
                </div>

                <div class="tech-footer">
                    <p>© 2025 Viettech Electronics. Tất cả quyền được bảo lưu.</p>
                    <p>Liên hệ hỗ trợ kỹ thuật: <span style="color: var(--primary-color); font-weight: 500;">admin@viettech.vn</span></p>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Xử lý sự kiện hiển thị/ẩn mật khẩu
        document.getElementById('togglePassword').addEventListener('click', function () {
            const passwordInput = document.getElementById('password');
            const icon = this.querySelector('i');

            if (passwordInput.type === 'password') {
                passwordInput.type = 'text';
                icon.classList.remove('fa-eye');
                icon.classList.add('fa-eye-slash');
            } else {
                passwordInput.type = 'password';
                icon.classList.remove('fa-eye-slash');
                icon.classList.add('fa-eye');
            }
        });

        // Xử lý đăng nhập
        document.getElementById('loginForm').addEventListener('submit', function (event) {
            event.preventDefault();

            const email = document.getElementById('email').value;
            const password = document.getElementById('password').value;

            // Hiển thị hiệu ứng loading
            const loginButton = document.querySelector('.btn-viettech');
            const originalText = loginButton.innerHTML;
            loginButton.innerHTML = '<span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>Đang xử lý...';
            loginButton.disabled = true;

            // Gửi yêu cầu POST đến API login
            fetch('/login', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({
                    email,
                    password,
                    remember: document.getElementById('rememberMe').checked
                }),
            })
                .then(response => response.json())
                .then(data => {
                    // Khôi phục trạng thái nút
                    loginButton.innerHTML = originalText;
                    loginButton.disabled = false;
                   
                    

                    if (data.status === "error") {
                        // Nếu có lỗi, hiển thị thông báo lỗi
                        document.getElementById('errorMessage').style.display = 'block';
                        document.getElementById('errorText').textContent = data.message;
                    } else {
                        localStorage.setItem('accessToken', data.result.metadata.tokens.accessToken);
                        localStorage.setItem('userId', data.result.metadata.account._id);
                        localStorage.setItem('refreshToken', data.result.metadata.tokens.refreshToken);
                        localStorage.setItem('role', data.result.metadata.account.role); // <== Thêm dòng này


                        console.log("data", data.result.metadata.account._id);
                        console.log("data 2", data.result.metadata.tokens.accessToken);
                        console.log("Refresh Token:", data.result.metadata.tokens.refreshToken);
                        console.log("Role:", data.result.metadata.account.role); // <== Thêm dòng này
                        
                        // Nếu đăng nhập thành công, chuyển hướng đến trang dashboard
                        window.location.href = data.redirectUrl;  // Đảm bảo rằng API trả về trường redirectUrl
                    }
                })
                .catch(error => {
                    // Khôi phục trạng thái nút
                    loginButton.innerHTML = originalText;
                    loginButton.disabled = false;

                    // Hiển thị lỗi kết nối
                    document.getElementById('errorMessage').style.display = 'block';
                    document.getElementById('errorText').textContent = 'Lỗi kết nối máy chủ. Vui lòng thử lại sau.';
                    console.error('Error:', error);
                });
        });
    </script>
</body>

</html>